workers:
  codex_count: 5

worktree:
  enabled: true
  default_base: ""
  branch_prefix: "yamibaito"

codex:
  sandbox: workspace-write
  approval: on-request
  model: default
  web_search: false

policies:
  tests: none
  shared_files: warn
  constraints_level: loose
